<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/style.css">
    <title>Index</title>
</head>
<body>
    <h1>Logs Index Page as of <%= time %> </h1>

    <ul class="logs-index">
        <% for (let i =0; i < logs.length; i++) { %>
            <li>
                The title of this log is:
                <a href="/logs/<%=logs[i].id; %>">
                     <%=logs[i].title; %>
                </a>
            <!-- DELETE FORM -->
            <form action="/logs/<%=logs[i].id; %>?_method=DELETE" method="POST">
                <input type="submit" value="DELETE">
            </form>

            <br>
        </li>
        <% } %>
    </ul>

    <a href="/logs/new"><button>Create A New Log</button></a>
</body>
</html>
