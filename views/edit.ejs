<!DOCTYPE html>
<html>
    <head>
        <meta charset="utf-8">
        <title></title>
    </head>
    <body>
          <section class="hero is-primary is-small has-text-centered">
      <div class="hero-body">
        <p class="title is-large">Edit Captain's Log "<%= log.title %> </p>
        <p class="subtitle">Click The Log To Enter</p>

      </div>
    </section>
        <form>
    		<!--  NOTE: the form is pre-populated with values for the server-->
    		Title: <input type="text" name="title" value="<%=log.title%>"/><br/>
    		Entry: <input type="text" name="entry" value="<%=log.entry%>"/><br/>
    		Ship is broken?:
            <input type="checkbox" name="shipIsBroken"
                <% if(log.shipIsBroken === true){ %>
                    checked
                <% } %>
            />
            <br/>
            <input type="submit" name="" value="Submit Changes"/>
        </form>
    </body>
</html>
